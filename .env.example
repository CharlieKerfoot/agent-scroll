# Anthropic — used when --provider anthropic
ANTHROPIC_API_KEY=

# OpenAI — used when --provider openai or --embedder openai
OPENAI_API_KEY=

# OpenRouter — used when --provider openrouter
OPENROUTER_API_KEY=
